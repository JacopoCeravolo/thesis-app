// Debug utility to print out the available models on the Prisma client
import prisma from "../../lib/prisma";

export function printPrismaModels() {
  /* console.log("Available Prisma models:");
  console.log(Object.keys(prisma)); */
}
